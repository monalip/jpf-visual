package se.kth.shell;

/**
 * All frames that are used by the Shell should implement ShellFrame.
 * ShellFrame serves as a marker to separate which frames are part of the
 * shell and which could belong to the application being checked.<br> If this
 * sounds like a hack, you are correct. A better solution probably needs to be
 * devised for this.
 */
public interface ShellFrame{
}
